const { When, Then } = require("@badeball/cypress-cucumber-preprocessor");
import RoomsAdmin from "../../pages/rooms_administration/pageRoomsAdmin";

const roomsAdmin = new RoomsAdmin();

// this common step definition file can be used to have all common room administration related step definitions in one place
