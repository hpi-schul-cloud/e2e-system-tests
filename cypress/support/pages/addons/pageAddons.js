import Navigation from '../common_global/pageNavigation'

const navigation = new Navigation()

When('I go to AddOns overview', () => {
  navigation.goToAddonsOverview()
})

//Then('I would need a final confirmation', () => {
//})
